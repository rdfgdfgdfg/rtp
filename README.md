# rtp
Recursive thread pool. Use tree-structure to assign resource.

# where to use
Very large and complex problems that need recursive algorithm. 
Have to use multithreading.
In a thread, child-thread should be assign equal sources
